// Find second largest element in an array
#include <iostream>
#include <array>
using namespace std;

int secondLargest(int arr[], int n){

}

int main(){
    int n, arr[n];
    cin>>n;
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    cout<< secondLargest(arr,n);
    return 0;
}